// state
const initState = () => {
  return {
    value: '',
  };
};

// getters
const getters = {
  value: state => {
    return state.value;
  },
};

// actions
const actions = {
};

// mutations
const mutations = {
  setValue(state, value) {
    state.value = value;
  },
};

export default {
  state: initState,
  getters,
  actions,
  mutations,
};
